<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication Redirect</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin-top: 50px;
            background: linear-gradient(to right, #6a5acd, #8a2be2);
            color: white;
        }
        .container {
            margin: 0 auto;
            padding: 20px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            max-width: 500px;
        }
        .container h1 {
            margin-bottom: 20px;
        }
        .container p {
            margin-bottom: 30px;
        }
        .container a {
            padding: 10px 20px;
            background: #ffd700;
            color: #333;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s ease;
        }
        .container a:hover {
            background: #e5c100;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Authentication Required</h1>
        <p>You need to authenticate to continue. Please use the button below to sign in.</p>
        <a href="https://your-auth-domain.com/auth">Sign in with Google</a>
    </div>
</body>
</html>
